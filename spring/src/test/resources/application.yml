spring:
  persistent-tasks:
    scheduler-enabled: false
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false

  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: 

logging:
  level:
    org.sterl.spring.persistent_tasks: debug
    org.sterl.pmw: debug