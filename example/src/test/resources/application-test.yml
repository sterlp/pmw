spring:
  jpa:
    hibernate:
      ddl-auto: create
    open-in-view: false
  quartz:
    scheduler-name: TEST-PMW

  datasource:
    url: jdbc:h2:mem:test_mem;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: 
